# No rules: git and git-lfs are offline by default — this profile installs the tools
# and opens nothing. GitHub egress is its own grant: --with github is the anonymous
# public-read floor, and --with github-read:OWNER/REPO or --with
# github-write:OWNER/REPO grant one credentialed repository each.
